{
  "name": "portfolio-lucas-martinati",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cv:pdf": "node scripts/generate-cv-pdf.mjs",
    "deploy": "npm run cv:pdf && gh-pages -d dist -b gh-pages --dotfiles"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.0",
    "gh-pages": "^6.3.0",
    "puppeteer": "^25.12.0",
    "vite": "^6.0.0"
  }
}
